## 1. Add Section Structure to Right Panel

- [x] 1.1 Replace the single flat `grid` in the right panel with two `<section>` elements, each with a `<h3>` section header and its own `grid grid-cols-2 gap-4`
- [x] 1.2 Place Full Name, Email Address, Phone Number, and User Role inside the "Information" section grid
- [x] 1.3 Place Country, State/Region, City, ZIP/Postal Code, and Address (col-span-2) inside the "Address" section grid

## 2. Style Section Headers

- [x] 2.1 Apply consistent header style to both `<h3>` elements: `text-xs font-semibold text-muted-foreground uppercase tracking-widest mb-3`
