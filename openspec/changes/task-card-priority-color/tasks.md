## 1. Implement Priority Background on TaskCard

- [x] 1.1 Add a `PRIORITY_BG` lookup map in `KanbanBoard.tsx` mapping `"high"` → red class, `"medium"` → yellow class, `"low"` → green class
- [x] 1.2 Pass the matching background class (or empty string if no priority) to the `Card` component's `className` prop inside `TaskCard`

## 2. Verify

- [x] 2.1 Run `npm run build` and confirm no errors
